{
    "_comment": "InteropHQ voice node. Installs a prebuilt binary from a GitHub release; no compiler is needed at runtime or at install. Needs TWO allocations: the primary is the control API (FXServer -> node) and a second is the audio stream (players -> node).",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2026-09-07T00:00:00+00:00",
    "name": "InteropHQ Voice Node",
    "author": "avery627@gmail.com",
    "uuid": "a1ace3f1-0d6a-4222-9bc7-6085c49c9c1e",
    "description": "rfx-voiced - the P25 voice node. Taps each FXServer's built-in Mumble server, runs the vocoder chain, and streams radio audio to game clients. Pulls its server list from InteropHQ when a platform key is set, and falls back to a local servers.json when it is not.",
    "features": null,
    "docker_images": {
        "Debian (yolks)": "ghcr.io/parkervcp/yolks:debian"
    },
    "file_denylist": [],
    "startup": "VOICED_CONTROL=0.0.0.0:${SERVER_PORT} VOICED_STREAM=0.0.0.0:${VOICED_STREAM_PORT} ./rfx-voiced",
    "config": {
        "files": "{}",
        "startup": "{\"done\": \"server(s) from\"}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/bash\n# InteropHQ voice node - install\n#\n# Server Files: /mnt/server\n#\n# Two modes:\n#\n#   release  download the prebuilt binary from a GitHub release. Seconds, and\n#            no compiler, so it cannot run the panel out of memory. Default.\n#   source   clone and cargo build. Needs ~2GB and several minutes.\n#\n# Building on the panel was the original design and it was wrong: rustc runs\n# one process per core, each wanting hundreds of megabytes, and Pelican gives\n# the install container the server's memory limit. A 1GB server OOM-kills the\n# build partway through with SIGKILL and no explanation beyond \"signal: 9\".\n\nset -e\n\nexport DEBIAN_FRONTEND=noninteractive\napt-get update\napt-get install -y --no-install-recommends ca-certificates curl jq git\n\nmkdir -p /mnt/server\ncd /mnt/server\n\nexport HOME=/mnt/server\nexport CARGO_HOME=/mnt/server/.cargo\n\nif [[ ${GIT_ADDRESS} != *.git ]]; then\n    GIT_ADDRESS=${GIT_ADDRESS}.git\nfi\n\n# NEVER echo a URL after credentials have been spliced into it. The panel's\n# install log is visible to anyone with access to the server, is kept, and is\n# the first thing people paste when asking for help - so a token printed here\n# is a token to treat as public.\nREPO=$(echo \"${GIT_ADDRESS}\" | sed -E 's#.*github\\.com[:/]([^/]+/[^/]+)\\.git#\\1#')\nAUTH_URL=\"${GIT_ADDRESS}\"\nif [ -n \"${USERNAME}\" ] && [ -n \"${ACCESS_TOKEN}\" ]; then\n    AUTH_URL=\"https://${USERNAME}:${ACCESS_TOKEN}@$(echo -e ${GIT_ADDRESS} | cut -d/ -f3-)\"\nfi\n\nBRANCH=\"${BRANCH:-main}\"\nMODE=\"${INSTALL_MODE:-release}\"\n\ninstall_from_release() {\n    echo \"looking for the latest release of ${REPO}\"\n\n    local hdr=(-H \"Accept: application/vnd.github+json\")\n    [ -n \"${ACCESS_TOKEN}\" ] && hdr+=(-H \"Authorization: Bearer ${ACCESS_TOKEN}\")\n\n    local json\n    json=$(curl -fsSL \"${hdr[@]}\" \"https://api.github.com/repos/${REPO}/releases/latest\") || return 1\n\n    local tag asset_id\n    tag=$(echo \"${json}\" | jq -r '.tag_name // empty')\n    asset_id=$(echo \"${json}\" | jq -r \\\n        '.assets[]? | select(.name | endswith(\"x86_64-unknown-linux-gnu.tar.gz\")) | .id' | head -1)\n\n    [ -n \"${asset_id}\" ] || return 1\n    echo \"found ${tag}, asset ${asset_id}\"\n\n    # The asset endpoint, not browser_download_url: that one is unauthenticated\n    # and 404s on a private repository.\n    curl -fsSL \"${hdr[@]}\" -H \"Accept: application/octet-stream\" \\\n        \"https://api.github.com/repos/${REPO}/releases/assets/${asset_id}\" \\\n        -o /tmp/voiced.tar.gz || return 1\n\n    tar -xzf /tmp/voiced.tar.gz -C /tmp\n    local bin\n    bin=$(find /tmp -maxdepth 3 -type f -name rfx-voiced | head -1)\n    [ -n \"${bin}\" ] || return 1\n\n    cp \"${bin}\" /mnt/server/rfx-voiced\n    chmod +x /mnt/server/rfx-voiced\n    rm -rf /tmp/voiced.tar.gz\n    echo \"installed ${tag} from release\"\n}\n\ninstall_from_source() {\n    echo \"building from source\"\n    apt-get install -y --no-install-recommends cmake g++ curl build-essential\n\n    if ! command -v cargo >/dev/null 2>&1; then\n        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \\\n            | sh -s -- -y --profile minimal --no-modify-path\n        export PATH=\"${CARGO_HOME}/bin:${PATH}\"\n    fi\n\n    local src=/mnt/server/source\n\n    # The checkout is owned by the server's uid, and this script runs as root.\n    # Git refuses to touch a repository owned by somebody else - a sensible\n    # default on a shared machine, and exactly wrong here, where \"somebody\n    # else\" is the container this very install is for.\n    git config --global --add safe.directory '*'\n\n    if [ -d \"${src}/.git\" ]; then\n        echo \"updating existing checkout\"\n        cd \"${src}\"\n        git remote set-url origin \"${AUTH_URL}\"\n        git fetch --all --prune\n        git reset --hard \"origin/${BRANCH}\"\n    else\n        echo \"cloning ${REPO} (${BRANCH})\"\n        rm -rf \"${src}\"\n        git clone --depth 1 --single-branch --branch \"${BRANCH}\" \"${AUTH_URL}\" \"${src}\"\n    fi\n\n    cd \"${src}/${CRATE_DIR:-.}\"\n    [ -f Cargo.toml ] || { echo \"no Cargo.toml at ${CRATE_DIR:-.}\"; exit 1; }\n\n    # Two jobs, not one per core. Peak memory is roughly jobs x rustc, and the\n    # install container inherits the server's memory limit - unbounded\n    # parallelism is what turns a 1GB server into a SIGKILL halfway through\n    # compiling syn.\n    echo \"building with ${CARGO_JOBS:-2} job(s) - the slow part, and it only happens here\"\n    cargo build --release --jobs \"${CARGO_JOBS:-2}\"\n\n    cp target/release/rfx-voiced /mnt/server/rfx-voiced\n    chmod +x /mnt/server/rfx-voiced\n    rm -rf target\n}\n\nif [ \"${MODE}\" = \"source\" ]; then\n    install_from_source\nelif ! install_from_release; then\n    echo \"no usable release found - falling back to building from source\"\n    install_from_source\nfi\n\n# Leaving a broken install to be discovered at startup as \"No such file or\n# directory\" wastes the one place that could have said what went wrong.\nif [ ! -x /mnt/server/rfx-voiced ]; then\n    echo \"INSTALL FAILED - no binary at /mnt/server/rfx-voiced\"\n    exit 1\nfi\n\necho \"install complete - $(/mnt/server/rfx-voiced help | head -1)\"\nexit 0\n",
            "container": "debian:bookworm-slim",
            "entrypoint": "bash"
        }
    },
    "variables": [
        {
            "name": "Git repository",
            "description": "Repository holding the voice node source. HTTPS URL.",
            "env_variable": "GIT_ADDRESS",
            "default_value": "https://github.com/avery1227/rfx-voiced",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string"
        },
        {
            "name": "Branch",
            "description": "Branch to build.",
            "env_variable": "BRANCH",
            "default_value": "main",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string"
        },
        {
            "name": "Crate directory",
            "description": "Path to the crate inside the repository. Use . if the repo IS the crate; use resources/[local]/rfx_p25/voiced if you pushed the whole FiveM project.",
            "env_variable": "CRATE_DIR",
            "default_value": ".",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string"
        },
        {
            "name": "Install mode",
            "description": "release: download the prebuilt binary from the latest GitHub release - seconds, and no compiler to run the panel out of memory. source: clone and build, which needs about 2GB and several minutes.",
            "env_variable": "INSTALL_MODE",
            "default_value": "release",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|in:release,source"
        },
        {
            "name": "Build jobs (source mode)",
            "description": "Parallel rustc processes. Peak memory is roughly this times a few hundred MB, and the install container inherits the server's memory limit - raise only if the server has the RAM to match.",
            "env_variable": "CARGO_JOBS",
            "default_value": "2",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|integer|between:1,16"
        },
        {
            "name": "Git username (private repos)",
            "description": "Leave blank for a public repository.",
            "env_variable": "USERNAME",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string"
        },
        {
            "name": "Git access token (private repos)",
            "description": "Personal access token. Leave blank for a public repository.",
            "env_variable": "ACCESS_TOKEN",
            "default_value": "",
            "user_viewable": false,
            "user_editable": true,
            "rules": "nullable|string"
        },
        {
            "name": "Audio stream port",
            "description": "The SECOND allocation, in Network. Players' game clients connect here for audio, so unlike the primary port this one has to be reachable from outside the host.",
            "env_variable": "VOICED_STREAM_PORT",
            "default_value": "8788",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|integer|between:1,65535"
        },
        {
            "name": "Platform URL",
            "description": "InteropHQ, for the server list and heartbeat. Leave blank to run standalone from servers.json.",
            "env_variable": "VOICED_PLATFORM_URL",
            "default_value": "https://api.interophq.com",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string"
        },
        {
            "name": "Platform node key",
            "description": "NODE_API_KEY from the platform. This is a DIFFERENT principal from a server key - a node leaking must not be a server leaking. Watch for a trailing space; it goes inside the value and 401s.",
            "env_variable": "VOICED_PLATFORM_KEY",
            "default_value": "",
            "user_viewable": false,
            "user_editable": true,
            "rules": "nullable|string"
        },
        {
            "name": "Tap username",
            "description": "What the tap calls itself on each FXServer's Mumble server. FiveM names clients [id] name, so this must keep that shape.",
            "env_variable": "VOICED_USER",
            "default_value": "[999] radiotap",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string"
        },
        {
            "name": "Codec2 mode",
            "description": "3200 or 2400. 2400 is rate-matched to AMBE+2 half-rate; 3200 is the closer perceptual match to a real Phase 2 radio on a strong signal.",
            "env_variable": "VOICED_CODEC2_MODE",
            "default_value": "3200",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|integer|in:2400,3200"
        },
        {
            "name": "Server list cache",
            "description": "Last good platform response. The dashboard being down should degrade the platform, not stop it.",
            "env_variable": "VOICED_CACHE",
            "default_value": "/home/container/servers.cache.json",
            "user_viewable": true,
            "user_editable": false,
            "rules": "required|string"
        },
        {
            "name": "Local server store",
            "description": "Hand-enrolled servers, used when no platform is configured.",
            "env_variable": "VOICED_STORE",
            "default_value": "/home/container/servers.json",
            "user_viewable": true,
            "user_editable": false,
            "rules": "required|string"
        }
    ]
}
